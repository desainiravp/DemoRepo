name: Demo Repo CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Node.js (for example, if needed)
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Run a simple check
      run: |
        echo "Running demo repo CI workflow"
        ls -la
        cat README.md
